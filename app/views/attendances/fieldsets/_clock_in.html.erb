    <%if f.object.allow_clock_in? %>
      <%= f.input :clock_in_at, as: :datetime,
                                html5: true,
                                input_html: {
                                  class: 'form-control datepicker',
                                  value: DateTime.now.strftime('%Y-%m-%dT%H:%M')
                                  }
                                %>
    <% end %>